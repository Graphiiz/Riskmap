FROM golang:1.19 AS builder

WORKDIR /app

ENV DIR_PROJ=backend

COPY ${DIR_PROJ}/go.mod .
COPY ${DIR_PROJ}/go.sum .
RUN go mod download

COPY ${DIR_PROJ}/. .
RUN go build -o ./api

FROM alpine:3.17.0_rc1

WORKDIR /build

COPY --from=build /app/api ./api

ENTRYPOINT ["./api"]
